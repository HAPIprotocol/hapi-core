# Building the HAPI Explorer image

```sh
docker build -t hapi-explorer -f ./explorer/Dockerfile ..
```

# Building the HAPI Indexer image

```sh
docker build -t ars9/hapi-indexer -f ./indexer/Dockerfile ..
```
